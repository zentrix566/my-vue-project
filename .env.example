# 大模型 API 密钥（二选一，火山方舟优先）
# 本变量不带 VITE_ 前缀，仅在 Vite 开发/预览服务器的代理中读取，不会打包进前端产物。
# 复制本文件为 .env 并填入真实密钥；.env 已被 .gitignore 忽略，不会提交。
#
# 方案一（优先）：火山引擎方舟（Ark）Coding 接口（模型 ark-code-latest）
HUOSHAN_KEY=your-huoshan-api-key-here
#
# 方案二（回退）：DeepSeek 官方 API（https://api.deepseek.com，模型 deepseek-v4-flash）
DEEPSEEK_KEY=your-deepseek-api-key-here
#
# 可选：维基百科本地 HTTP 代理（用于访问被网络拦截的 wikipedia.org）
# 格式 host:port，默认 127.0.0.1:10808（v2rayN/Xray 默认端口）；不需要可留空或注释
# WIKI_PROXY=127.0.0.1:10808
