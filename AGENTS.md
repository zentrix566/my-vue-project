# AGENTS.md

本文件指导后续 AI（或人类协作者）如何向「历史时间轴」功能添加人物、皇帝、典故、事件或新朝代。改动前请通读本文件。

## 一、相关文件

| 文件 | 作用 |
|---|---|
| `src/features/history-timeline/data/chineseHistory.json` | 中国史主数据：朝代（era）→ 皇帝分组（group）→ 人物（figure）/ 典故（allusion），以及朝代级历史事件（event） |
| `src/features/history-timeline/data/personDetails.json` | 按人名索引的生卒年与生平摘要，被主数据通过人名查找 |
| `src/features/history-timeline/data/worldHistory.json` | 世界史（非中国）扁平数组，结构与中国史不同，不要套用本文规则 |
| `src/features/history-timeline/pages/HistoryTimeline.vue` | 渲染层。**改数据时不需要改此文件**；只有新增字段或调整展示逻辑时才需要动它 |
| `src/data/changelog.js` | 页面顶部的更新日志；历史时间轴的用户可见数据或交互改动必须同步补一条，详细格式见 `AGENTS-features.md` |

原始史料笔记位于用户文档目录：
- `D:\文档\我的文档\历史.md` —— 总纲，宋/辽/金/西夏等未单独成篇的朝代写在这里
- `D:\文档\我的文档\历史\汉朝.md`、`唐朝.md`、`明朝.md`、`清朝.md`、`隋朝.md`、`南北朝十六国.md`、`五代十国.md` —— 各朝分篇

`personDetails.json` 里的 `source` 字段就是这些文件名（如 `"汉朝.md"`、`"历史.md"`），新增条目时按史料来源填写。

---

## 八、与通用 feature 约定的关系

本文件只讲 `history-timeline` 这一支的**数据结构**。新增一个**全新子项目**（feature）的通用接入约定（manifest 契约、自动注册、复制到别的项目）见 **`AGENTS-features.md`**。往 history-timeline 里加人物 / 皇帝 / 典故 / 朝代走本文件；新建 feature 走 `AGENTS-features.md`。

## 二、数据结构

### 2.1 朝代（era）

```jsonc
{
  "key": "beisong",          // 英文唯一标识，用于路由/锚点/localStorage
  "name": "北宋",             // 显示名
  "tagline": "与士大夫共治天下", // 卡片副标题
  "start": 960, "end": 1127, // 公元前用负数（如 -202）
  "color": "#1565c0",        // 主题色，见下方「颜色」一节
  "minW": 580,               // 可选，分组最小宽度
  "intro": "……",             // 朝代简介
  "groups": [ Group ],       // 皇帝/时期分组
  "events": [ Event ],       // 朝代级历史事件
  "regimeOrder": ["后梁", ...] // 可选，多政权朝代右下角下拉的政权排列顺序
}
```

`eras` 数组顺序决定「快速跳转」下拉里的朝代顺序；时间轴本身按 `start` 年份排序，所以即使数组顺序与年代不完全一致，卡片也会排到正确位置，但**请尽量按建国年代排列**以方便阅读。朝代间无「`last`」字段，末帝是在 group 上标记的。

**`regimeOrder`**：可选字段，用于在右下角皇帝下拉里指定政权分组的排列顺序（如五代十国先列五代后列十国）。不填则按各政权最早皇帝即位年排序。仅对在 `REGIME_DROPDOWN` 集合中的朝代生效。

### 2.2 分组（group）

每个 group 代表一位皇帝（或一个时期）：

```jsonc
{
  "label": "太祖开国",        // 分组标签，同时用于排序（见 2.6）
  "ruler": "赵匡胤",          // 皇帝名（人名，非帝号），必须能在 personDetails.json 查到；无皇帝的过渡时期可设 null
  "rulerTitle": "北宋第1位皇帝·宋太祖", // 皇帝头衔，见下方「rulerTitle 命名约定」
  "start": 960, "end": 976, // 在位起止年（用于在位年数与排序）
  "blurb": "……",            // 皇帝简介兜底文案（见 2.5 的覆盖规则）
  "figures": [ Figure ],     // 该时期的大臣/将领/后妃等
  "allusions": [ Allusion ], // 可选，典故卡
  "last": true               // 可选，标记为该朝末代皇帝，卡片显示「末帝」徽章
}
```

**rulerTitle 命名约定（重要）：** 格式为「朝代 + 第N位皇帝 + · + 庙号/帝号」，例如 `北宋第1位皇帝·宋太祖`、`辽第6位皇帝·辽圣宗`、`金第4位皇帝·金海陵王`。**末代皇帝**统一用「朝代 + 第N位·末代皇帝」（不写庙号，因为多数末帝无庙号或被废），例如 `西汉第15位·末代皇帝`、`辽第9位·末代皇帝`、`金第10位·末代皇帝`。该朝代的 N 按实际在位顺序连续编号；被追尊但未实际在位者（如曹操、司马懿）不计入。

**关键约定：**

- 每位正式皇帝应单独成组，不要把多任皇帝合并成一个无 `ruler` 的块（旧数据里的「理度」「宋末」是反面教材，已在 2026-08 拆分）。
- 在位年份务必准确。皇帝**禅位/被废/死亡**的年份是其在位 `end`，不是朝代灭亡年。例如宋徽宗 1125 年禅位，其 group `end` 应为 1125；宋钦宗 1125–1127 才是末帝。
- 每个政权的最后一任实际皇帝必须加 `"last": true`。单政权朝代（如唐、宋、明）全朝只有一个 `last: true`；多政权并立的朝代（十六国、南北朝、五代十国）每个政权各有一个末帝标记。注意：是「实际末帝」而非名义上的——如金哀宗自缢前传位末帝完颜承麟，末帝才是 `last: true`。
- 无皇帝的过渡/摄政分组（如伪楚、短期傀儡）可用 `ruler: null`，但应尽量少用。

### 2.3 人物（figure）

```jsonc
{
  "name": "杨业",             // 查找键，必须与 personDetails.json 的 key 完全一致
  "role": "杨老令公",         // 身份标签，显示在卡片上
  "note": "号\"杨无敌\"……",  // 简评兜底文案（见 2.5 覆盖规则）
  "year": 986,                // 可选，显式指定在时间轴上的年份；不填则用 group.start
  "life": "？—986年",         // 可选，内联生卒年（一般写进 personDetails 即可，不必在这里重复）
  "custom": true              // 仅运行时由 localStorage 添加的自添人物使用，静态 JSON 不要写此字段
}
```

### 2.4 典故（allusion）与事件（event）

```jsonc
// allusion：挂在某个 group 下，以「📖 典故」卡形式出现在皇帝卡旁
{ "name": "杯酒释兵权", "year": 961, "note": "……" }

// event：挂在 era 下，作为独立的「历史事件」卡出现在时间轴上
{ "name": "靖康之变", "year": 1127, "note": "……" }
```

**怎么区分：**

- 典故（allusion）是文化/政治/典故性的小故事，依附于某位皇帝（如「故剑情深」「十二道金牌」「莫须有」「烛影斧声」），放在 group 内。
- 事件（event）是改朝换代、战争、盟约等朝代级节点（如「靖康之变」「澶渊之盟」「崖山海战」「海上之盟」），放在 era 下。
- 一个主题只放一处，不要既当典故又当事件造成重复。涉及两个朝代的事件（如澶渊之盟属宋辽、海上之盟属宋金），选**主要受益方或史料描述所在朝代**放置即可，不必两边都加。

### 2.5 personDetails.json 与「摘要覆盖」规则

文件是一个以人名为 key 的扁平对象：

```jsonc
"岳飞": {
  "life": "1103年—1142年",
  "source": "历史.md",
  "summary": "南宋抗金名将……"
}
```

- `life`：生卒年文本，格式见 2.7。
- `source`：来源 md 文件名。
- `summary`：生平摘要。**渲染层有一条覆盖规则**（`HistoryTimeline.vue` 的 `description()` 函数）：
  - 如果 `summary` 存在且**不包含字符 `…`**，卡片和详情面板显示 `summary`，**忽略** JSON 里的 `figure.note` / `group.blurb`。
  - 如果 `summary` 为空、或包含 `…`（表示信息不全），则**回退**显示 `figure.note`（对人物）或 `group.blurb`（对皇帝）。
- 实际推论：
  - 给皇帝/人物写了完整 `summary`，就以 `summary` 为准；此时 `blurb`/`note` 只在 lookup 失败时兜底，两者内容可互补但不要矛盾。
  - 如果希望时间轴显示 `note`/`blurb`、personDetails 里只存生卒年，就把 summary 写成带 `…` 的截断句，或干脆不写 summary（只写 life/source）。
- **皇帝也必须登记 personDetails**：group 的 `ruler` 名会被拿去查 personDetails，查到就用其 `life` 显示生卒/享年、用其 `summary` 覆盖 blurb。即使只知道生卒年，也应建一条 `{ "life": "...", "source": "...", "summary": "..." }`。

### 2.6 分组排序与标签

时间轴排序优先级（见 `HistoryTimeline.vue` 的 `allItems` sort）：

1. 按 `group.start`（或 figure/event.year）升序；
2. 同年按 `eraIndex`（数组顺序）；
3. 同年代按 `labelOrder(label)`——用正则取 label 里第一个数字；
4. 再按 `group.end`。

因此：

- 给同一朝代的 group 命名时，让 label 带有序号最稳妥（如西汉的「西汉1」、辽/金的「辽1」「金10·末代皇帝」），也可用描述性名字（如北宋的「太祖开国」「仁宗盛治」「神宗变法」——这些 label 没有数字，`labelOrder` 返回 0，靠 start 年排序，只要年份不冲突就没问题）。
- **多政权并立的朝代**（东晋十六国、南北朝、五代十国）的 group label 必须带政权前缀（如「后梁1」「北魏1」「汉赵1」「前秦3」），`regimeOf(label)` 会去掉末尾数字得到政权名（如「后梁」「汉赵」），供右下角皇帝下拉分组使用。
- 时间轴上的排列顺序由 `HistoryTimeline.vue` 顶部的两个集合控制：
  - `PARALLEL`：在时间轴上**按政权整块排列**的朝代（目前只有 `nanbeichao`，因南北朝南北两条线较清晰）。集合外的多政权朝代（十六国、五代十国）在时间轴上**纯按年份交错排列**，读起来是一条连续时间线。
  - `REGIME_DROPDOWN`：在右下角「选择皇帝」下拉里**按政权用 `<optgroup>` 分组**的朝代（`shiliuguo`、`nanbeichao`、`wudai`）。即便时间轴按年份排，下拉里同朝皇帝仍会聚在一起方便跳转。
  - 新增多政权并立朝代时，按需把 era.key 加进这两个集合——**这属于改 Vue 代码，不是纯数据改动**。

### 2.7 生卒年格式与享年自动计算

`life` 字段会被 `parseLifeYear()` / `lifeAge()` 解析以自动计算「享年」。兼容写法：

- 分隔符可用：`—` `–` `－` `~` `～` `-`（任一种）
- 公元后直接写年份：`"1103年—1142年"`
- 公元前加「前」或「公元前」：`"前256年—前195年"`、`"公元前536年—公元前448年"`
- 不确定年份用「约」：`"约984年—约1053年"`（会自动去掉「约」再解析）
- 生年或卒年不详用「？」或「不详」：`"？—前196年"`、`"不详—前1年"`（**含「？」的一段返回 null，该段不参与计算**，故享年留空不显示）
- 多版本用「/」分隔，取第一个：`"前256年/前247年－前195年"`（解析为前 256 年生）
- 跨公元前/公元的享年按 `|生| + |卒| - 1` 计算（扣减公元 0 年）

写成 `"939年11月20日－997年5月8日"` 这种带月日的也能解析（正则只取第一个 `\d+年`）。

如果 `life` 缺字段或解析失败，享年不显示——这是正常现象，不要为了显示享年而臆造生卒年。

## 三、常见操作步骤

### 3.1 添加一个人物（最常见）

1. 确定该人物属于哪位皇帝/哪个时期，找到 `chineseHistory.json` 里对应的 group。
2. 在 group 的 `figures` 数组里加：
   ```json
   { "name": "秦桧", "role": "权相·主和", "note": "……" }
   ```
   - `name` 必须与 personDetails 的 key 一字不差（注意异体字、繁简、帝号）。
   - `role` 用简洁的身份标签（如「抗金名将」「权相」「断腕皇后」），不要写成长句。
   - `note` 写一到两句关键事迹/结局。
3. 打开 `personDetails.json`，加一条：
   ```json
   "秦桧": { "life": "1090年—1155年", "source": "历史.md", "summary": "……" }
   ```
   - 生卒年不详就写 `"？—年份"` 或 `"不详—年份"`。
   - `summary` 写得比 `note` 更完整；若只想用 `note`、不在详情里显示长摘要，可把 summary 留短或带 `…`。
4. 验证：见第四节。

### 3.2 添加一位皇帝（新 group）

1. 在对应朝代的 `groups` 数组里按即位年插入新 group（参考 2.2 字段）。
2. `rulerTitle` 用庙号/帝号（如「宋太宗」「辽天祚帝」）。
3. `start`/`end` 填在位起止年。
4. 如果是该朝最后一位皇帝，加 `"last": true`，并确保之前的「末帝」标记移到它身上（**一个朝代只能有一个 `last: true`**）。
5. 在 `personDetails.json` 里补这位皇帝的生卒年与摘要。
6. 如果该时期有重要人物/典故，同时补 figures/allusions。

### 3.3 添加一个新朝代（新 era）

1. 在 `eras` 数组按建国年位置插入 era 对象（参考 2.1）。
2. `key` 用小写英文/拼音，且与其它 key 不重复。
3. 选一个未被相邻朝代用过的 `color`（见下方「颜色」表）。
4. 为每个皇帝建 group、为每个 group 按需加 figures/allusions、在 era 下加 events。
5. 为所有 ruler 和 figure 在 personDetails.json 建档。
6. 如果是多政权并立的朝代（如十六国、五代十国这类同一时期多个政权），group label 必须带政权前缀；默认时间轴会按年份交错排列。若希望时间轴上按政权整块排列（如南北朝），把 era.key 加进 `PARALLEL`；若希望右下角皇帝下拉按政权分组，把 era.key 加进 `REGIME_DROPDOWN`。这两个集合在 `HistoryTimeline.vue` 顶部，改动属于改 Vue 代码。
7. 页面顶部的「快速跳转」下拉和右下角浮动的朝代下拉会自动包含新朝代；右下角皇帝下拉在非分组朝代为扁平列表，在 `REGIME_DROPDOWN` 朝代里按政权 `<optgroup>` 分组，无需改模板。

### 3.4 添加历史事件 / 典故

- 朝代级战争、盟约、政变 → 加在 era 的 `events` 数组。
- 与某位皇帝直接相关的典故、成语、轶事 → 加在 group 的 `allusions` 数组。
- 二者都要有 `name`、`year`、`note`。

## 四、颜色

已用颜色（按 era 顺序）。新增朝代时挑一个与左右相邻朝代不同、且对比度足够（白字可读）的颜色：

| 朝代 | 颜色 | | 朝代 | 颜色 |
|---|---|---|---|---|
| 上古 | `#7a5c43` | | 唐 | `#ad1457` |
| 夏 | `#6b4f3a` | | 五代 | `#757575` |
| 商 | `#a8743a` | | 辽 | `#00695c` |
| 西周 | `#827717` | | 北宋 | `#1565c0` |
| 春秋 | `#00796b` | | 金 | `#bf360c` |
| 战国 | `#d84315` | | 南宋 | `#283593` |
| 秦 | `#455a64` | | 元 | `#5d4037` |
| 西汉 | `#c62828` | | 明 | `#b8860b` |
| 东汉 | `#e65100` | | 清 | `#5e35b1` |
| 三国 | `#6a4fa3` | | 民国 | `#546e7a` |
| 西晋 | `#607d8b` | | 当代 | `#d32f2f` |
| 东晋 | `#00838f` | | | |
| 十六国 | `#4e342e` | | | |
| 南北朝 | `#7e57c2` | | | |
| 隋 | `#2e7d32` | | | |

## 五、验证步骤（每次改完数据都要做）

在项目根目录运行：

```bash
# 1. JSON 合法性 + 结构自检（两个文件都要能被 require）
node -e "const h=require('./src/features/history-timeline/data/chineseHistory.json'); const d=require('./src/features/history-timeline/data/personDetails.json'); console.log('eras:', h.eras.length, 'details:', Object.keys(d).length)"

# 2. 检查每个 ruler 和 figure 是否都有 personDetails 条目（漏填的会回退到 blurb/note，但生卒年/享年会缺失）
node -e "
const h=require('./src/features/history-timeline/data/chineseHistory.json');
const d=require('./src/features/history-timeline/data/personDetails.json');
const miss=[];
for(const e of h.eras) for(const g of e.groups){
  if(g.ruler && !d[g.ruler]) miss.push(e.key+'/'+g.ruler);
  for(const f of (g.figures||[])) if(!d[f.name]) miss.push(e.key+'/'+f.name);
}
console.log(miss.length ? 'MISSING:\n'+miss.join('\n') : 'all rulers/figures have details');
"

# 3. 构建通过
npm run build
```

构建成功后可用 `npm run dev` 打开浏览器，在时间轴页搜索新人物、点开卡片核对生卒年/享年/典故显示是否正确。

## 六、自添人物（localStorage）与静态数据的区别

页面上「添加人物」按钮让用户自己加人物，这些存在浏览器 `localStorage`（key 为 `history-timeline:custom-people`），**运行时追加到对应朝代最后一个 group**，不会写回 JSON。

- 如果你是在帮用户把**史料笔记里的人物**补进时间轴，应当直接改 `chineseHistory.json` + `personDetails.json`（静态数据），而不是用 localStorage。
- `figure.custom: true` 只用于 localStorage 自添人物，静态 JSON 里不要写这个字段。

## 七、注意事项与常见陷阱

1. **不要臆造生卒年**。史料没写就用「？」「不详」，宁可享年留空。
2. **一个朝代只能有一个 `last: true`**。新增末帝时记得把旧标记移走。
3. **人名必须逐字一致**。`personDetails.json` 是用名字字符串精确匹配的，「刘彻」≠「汉武帝」、「赵炅」≠「赵匡义」（后者是曾用名）。group.ruler 填的是**人名**（如「赵炅」「赵匡胤」），rulerTitle 填带序号的帝号（如「北宋第2位皇帝·宋太宗」）。
4. **summary 会覆盖 note/blurb**。改了 `note` 但页面没变，先检查 personDetails 里有没有同名条目的 `summary` 在起作用。
5. **公元前后年份用负数**：公元前 202 年写 `-202`，公元后直接写 `960`。
6. **不要重复事件**。涉及多国的事件（澶渊之盟、海上之盟、联蒙灭金）只在一个朝代放。
7. **保持中文风格一致**：role 标签简短（二到六字），note 用书面白描，标点用中文全角，不要口语化网络用语；可保留史料里生动的说法（如「高梁河车神」「蟋蟀宰相」「睡王」）。
8. **`source` 字段填对应的 md 文件名**，不要空着。宋辽金夏的内容来自 `历史.md`，其余来自对应的分篇文件。
9. **不要破坏现有 JSON 格式**：2 空格缩进、文件末尾留一个空行、字符串里的引号正确转义。
10. **世界史（worldHistory.json）是另一套结构**，本文规则不适用；改世界史时单独读那个文件。

## 八、Git 规则（来自用户编程规范，强制）

- **永远不要 `git push`**，任何情况下都不得推送到远程仓库。需要推送由用户自己执行。
- **不要自动 commit**。改完数据停在「已修改未提交」状态，向用户汇报即可。只有用户在**当前这一轮**明确说「提交/commit」时才执行 `git add` / `git commit`，上一轮说过不算。
- commit message 用中文，格式 `<类型>: <中文描述>`，如 `feat: 时间轴补全两宋皇帝与辽金朝代`。
- 作者只能是 `zentrix566 <zentrix566@gmail.com>`，不得加任何 AI 联名作者。

## 九、本次（2026-08）改动记录（供参考）

- 北宋：补全太宗赵炅、英宗赵曙、钦宗赵桓三位皇帝；修正太祖在位年（960–976）、徽宗禅位年（至 1125）；新增杨业移入太宗组、杨文广、方腊；新增杯酒释兵权、烛影斧声、高梁河之战、最后封禅、熙宁变法等典故。
- 南宋：补全光宗、宁宗、理宗、度宗、恭帝、端宗、末帝七位皇帝（原「理度」「宋末」无 ruler 块已拆分）；新增秦桧、韩世忠、张邦昌、刘豫；新增十二道金牌、莫须有、隆兴北伐、头骨酒器等典故与绍兴和议、联蒙灭金事件。
- 新增「辽」（9 帝）与「金」（10 帝）两个朝代，含述律平、耶律察割、萧绰、胡沙虎等人物与契丹建国、燕云十六州、海上之盟、金灭辽、蔡州之战等事件。
- 共新增约 44 条 personDetails 条目。

### 2026-08 第二轮改动

- 拆分原「东晋·十六国」为两个独立朝代：「东晋」（11 帝 + 桓楚，颜色 `#00838f`）与「十六国」（16 政权 54 位君主，新颜色 `#4e342e`），时间轴上按年份交错，以颜色区分南方/北方。
- 东晋：rulerTitle 统一为「东晋第N位皇帝·庙号」格式，末帝司马德文标 `last: true`；新增桓楚 group（桓玄，403–404，无序号，仿武周模式）；东晋1 补王敦、东晋9 补桓温；事件保留淝水之战、刘裕北伐。
- 十六国：从原 dongjin era 拆出 54 个 group；事件保留桓温灭成汉、参合陂之战；`REGIME_DROPDOWN` 中将 `dongjin` 替换为 `shiliuguo`（东晋只有一个政权无需分组下拉）。
- 五代十国：新增 era 级 `regimeOrder` 字段，右下角皇帝下拉政权分组按「后梁→后唐→后晋→后汉→后周→前蜀→…→北汉」排列（五代在前，十国在后）；`fabRulers` 支持该字段，未定义时回退到按即位年排序。
- 共新增 65 条 personDetails 条目（东晋 10 帝 + 王敦、桓玄、谢安、谢玄，以及十六国 50 位君主）。

### 2026-08 第三轮改动（补充割据政权与缺漏君主）

- 十六国：前凉从 4 位补全至 9 位（新增张轨、张茂、张耀灵、张祚、张玄靓，修正张寔年份）；汉赵补刘和、刘粲（含靳准 figure）；后赵补石世、石遵、石鉴；北燕补慕容云/高云。
- 新增五个短政权：冉魏（冉闵，350–352，杀胡令）、西燕（7 位君主，384–394）、代国（拓跋猗卢、拓跋什翼犍，315–376，北魏前身）、翟魏（翟辽、翟钊，388–392）、谯蜀（谯纵，405–413）；新增「北魏统一北方」（439）事件。
- 东汉：在新莽与东汉1之间插入玄汉 group（刘玄，23–25，含刘盆子 figure）。
- 南北朝：南朝宋补刘义符（9 帝）、齐补萧昭文（7 帝）、梁补萧栋、萧渊明及侯汉（侯景，无序号仿桓楚模式，6 帝）；北魏从 10 位补全至 16 位（拓跋余、元姑娘、元钊、元晔、元恭、元朗）；西魏补元钦（3 帝）。
- 东晋：东晋5 加王羲之 figure、东晋10 加刘裕 figure。
- 共新增 38 条 personDetails 条目（总计 433 条）。
